/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Clean URLs are handled automatically by Next.js
  trailingSlash: false,
}

module.exports = nextConfig
